# fix: clamp negative counts to zero

Repository: `fixturerepo` (checked out at a fixed commit in `/app`)

Counts below zero clamp to zero instead of passing through.

Implement the change described above, following the repository's existing patterns. Your work will be validated by hidden verification tests.
